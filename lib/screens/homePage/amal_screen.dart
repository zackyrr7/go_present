import 'package:flutter/material.dart';

class AmalScreen extends StatelessWidget {
  const AmalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
